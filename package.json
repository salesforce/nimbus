{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.15.0"
  },
  "scripts": {
    "prepare": "lerna bootstrap",
    "lint": "lerna run lint",
    "start:demo": "lerna bootstrap && pushd packages/demo-wwww && npm run serve"
  }
}
