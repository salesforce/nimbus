apply plugin: 'maven-publish'

java {
    withJavadocJar()
    withSourcesJar()
}

artifactoryPublish.dependsOn("build")

afterEvaluate {
    publishing {
        publications {
            mavenPublication(MavenPublication) {
            }
        }
    }
}
